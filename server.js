var app = require('./server-config.js');

var port = 4568;

app.listen(port);

console.log('Server now listening on port ' + port);
console.log('testing change on local to live')
console.log('testing live change two')
console.log('this is another test')